[package]
name = "tardigrade-test"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
futures = "0.3.21"
serde = { version = "1", features = ["derive"] }

tardigrade = { version = "0.1.0", path = "../.." }

[dev-dependencies]
chrono = "0.4.19"
wasmtime = "0.37.0"
tardigrade-rt = { version = "0.1.0", path = "../../crates/rt" }
